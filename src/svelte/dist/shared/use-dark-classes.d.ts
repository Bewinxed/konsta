/**
 * A hook that returns a function to conditionally apply dark mode classes.
 *
 * @returns {Function} A function that conditionally returns dark mode classes.
 */
export function useDarkClasses(): Function;
